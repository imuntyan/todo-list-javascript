This project has been tested with node 10.23.0 and npm version 6.14.8

* Install the packages:

```shell script
npm install .
```
* Run tests:

```shell script
npm run test
```

* Run cli:

```shell script
node .
```